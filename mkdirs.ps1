if (!(Test-Path alib\obj)) { mkdir alib\obj | Out-Null }
if (!(Test-Path alib\lib)) { mkdir alib\lib | Out-Null }
if (!(Test-Path csvfix\obj)) { mkdir csvfix\obj | Out-Null }
if (!(Test-Path csvfix\bin)) { mkdir csvfix\bin | Out-Null }
